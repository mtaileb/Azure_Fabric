# Ce qui suit se fait dans Azure Powershell (et non le Bash):
md terraform
cd terraform
# Récupérer les fichiers de configuration Terraform:
$files = @('main.tf', 'terraform.tfvars', 'variables.tf', 'keyvault.tf', 'providers.tf')
$baseUri = "https://raw.githubusercontent.com/mtaileb/Azure_Fabric/main/Data_Factory/TP6_Provisioning_ADF_avec_Terraform/"
$files | ForEach-Object {
    $uri = "$baseUri$_"
    Invoke-WebRequest -Uri $uri -OutFile $_
}

